import 'react';

export default function PomodoroFooter ({ className, time, isTimerRunning, onTimerAction, onCancelAction }) {
    return (
        <sectiom className="">
           
        </sectiom>
    );
}